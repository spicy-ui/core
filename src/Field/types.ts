export interface FieldProps {
  errorText?: React.ReactNode;
  helperText?: React.ReactNode;
  id: string;
  isDisabled?: boolean;
  isInvalid?: boolean;
  isReadOnly?: boolean;
  isRequired?: boolean;
  label?: string;
}
