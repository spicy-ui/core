import styled from 'styled-components';
import { baseStyle } from '../../helpers';
import { Box } from '../Box';

export const Menu = styled(Box)`
  ${baseStyle('components.Menu')}
`;

export const MenuItem = styled(Box)`
  ${baseStyle('components.MenuItem')}
`;

export const MenuDivider = styled(Box)`
  ${baseStyle('components.MenuDivider')}
`;

export const MenuHeader = styled(Box)`
  ${baseStyle('components.MenuHeader')}
`;
